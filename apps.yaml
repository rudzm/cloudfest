apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  project: default
  source:
    path: examples/argocd/my-app
    repoURL: https://github.com/rudzm/cloudfest
    targetRevision: test
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
